export const DATE_TIME_FORMAT = 'DD/MM/YYYY HH:mm'

export enum TicketStatus {
  SCHEDULED = 'scheduled',
  ON_HOLD = 'on_hold',
  FINISHED = 'finished'
}

export enum EntityStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive'
}
